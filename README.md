A simple program to convert html to jpg. 

``````
Usage: node index.js [options]

Options:
  -d, --dir <dir>        the directory of html to conver
  -o, --output <output>  the directory to export the image
  -h, --help             output usage information
``````

